package it.polimi.ingsw.model;

public class Printer {
    public Printer() {
    }

    public static void printTile(Tile tile) {
        if (tile.getColor() == Color.GREEN) {
            //TODO : print the sprite of the colored tile
        }
    }
}
